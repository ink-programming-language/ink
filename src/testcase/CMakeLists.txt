file(GLOB INK_TOKENIZER_TEST_SOURCES CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/tokenizer/*_test.cpp")
list(SORT INK_TOKENIZER_TEST_SOURCES)

add_executable(ink_tests ${INK_TOKENIZER_TEST_SOURCES})
add_dependencies(ink_tests ink_tokenize)
target_compile_features(ink_tests PRIVATE cxx_std_17)
target_compile_definitions(ink_tests PRIVATE INK_LEXICAL_RULES_PATH="${PROJECT_SOURCE_DIR}/docs/Ink-Lexical-Rules.md")
target_link_libraries(ink_tests PRIVATE ink::core ink::tokenizer GTest::gtest_main)
ink_disable_exceptions(ink_tests)

if(MSVC)
  target_compile_options(ink_tests PRIVATE /utf-8)
endif()

include(GoogleTest)
gtest_discover_tests(ink_tests DISCOVERY_TIMEOUT 60)
file(GLOB INK_PARSER_TEST_SOURCES CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/parser/*_test.cpp")
add_executable(ink_parser_tests ${INK_PARSER_TEST_SOURCES})
add_dependencies(ink_parser_tests ink_parse)
target_link_libraries(ink_parser_tests PRIVATE ink::parser GTest::gtest_main)
ink_disable_exceptions(ink_parser_tests)
if(MSVC)
  target_compile_options(ink_parser_tests PRIVATE /utf-8)
endif()
gtest_discover_tests(ink_parser_tests DISCOVERY_TIMEOUT 60)
add_test(NAME ParserProcessTest COMMAND "${CMAKE_COMMAND}" "-DINK_PARSER=$<TARGET_FILE:ink_parse>" "-DINK_TEST_DIRECTORY=${CMAKE_CURRENT_BINARY_DIR}/parser-process" -P "${CMAKE_CURRENT_SOURCE_DIR}/parser/process_test.cmake")
add_test(NAME TokenizerProcessTest COMMAND "${CMAKE_COMMAND}" "-DINK_TOKENIZER=$<TARGET_FILE:ink_tokenize>" "-DINK_TEST_DIRECTORY=${CMAKE_CURRENT_BINARY_DIR}/tokenizer-process" -P "${CMAKE_CURRENT_SOURCE_DIR}/tokenizer/process_test.cmake")

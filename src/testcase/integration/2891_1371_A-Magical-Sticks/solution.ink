// Translated from solution.cpp.

func main()
{
  var n: dynamic;
  var temp: dynamic;
  read(n);
  {
    var i = 0;
    while ((i < n))
    {
      read(temp);
      write((((temp + 1)) / 2), "\n");
      i += 1;
    }
  }
}

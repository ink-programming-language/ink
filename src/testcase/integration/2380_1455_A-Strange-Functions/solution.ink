// Translated from solution.cpp.

var fastio = cpp_expression("#include<bits/stdc++.h> #include<");

func main()
{
  fastio;
  var s: dynamic;
  var t: dynamic;
  var n: dynamic;
  read(t);
  while (cpp_update(t, "--"))
  {
    read(s);
    n = s.size();
    write(n, "\n");
  }
  return 0;
}
